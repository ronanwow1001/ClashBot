discord_token = ''
# we keep the link regex secret
link_regex = """"""

logs_id = ''

# allowed domains, sub-domains of these will also work
allowed_domains = [
    'projectalt.is',
    'projectaltis.com'
]